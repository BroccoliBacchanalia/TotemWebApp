import React from 'react';
import * as actions from '../../../client/src/redux/actions/authenticationActions';

describe('Authentication Reducer', () => {
	xit('is waiting to be tested', () => {
		
	});
});
